// VectorComponent.js
class VectorComponent {
    constructor({ x, y, velocityX, velocityY }) {
        this.x = x; // X position
        this.y = y; // Y position
        this.velocityX = velocityX; // X velocity
        this.velocityY = velocityY; // Y velocity
    }

}

export default VectorComponent;